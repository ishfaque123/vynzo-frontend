'use client'; import {useEffect,useState} from 'react'; import {fetchAdminOverview} from '@/lib/api/adminApi'; import Link from 'next/link';
function Stat({label,value,detail}:{label:string;value:number;detail?:string}){return <div className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-slate-900">{value}</p>{detail&&<p className="mt-1 text-xs text-slate-500">{detail}</p>}</div>}
export default function Page(){const[data,setData]=useState<any>();const[error,setError]=useState('');useEffect(()=>{fetchAdminOverview().then(r=>r.success?setData(r.data):setError(r.error?.message||'Unable to load dashboard.'))},[]);if(error)return <div className="rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-700">{error}</div>;if(!data)return <div className="space-y-4"><div className="h-24 animate-pulse rounded-2xl bg-slate-200"/><div className="grid gap-3 md:grid-cols-4">{[1,2,3,4].map(x=><div key={x} className="h-28 animate-pulse rounded-2xl bg-slate-200"/>)}</div></div>;const c=data.counts;return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Dashboard</h2><p className="mt-1 text-sm text-slate-500">Platform overview and recent activity.</p></div><div className="grid grid-cols-2 gap-3 md:grid-cols-4"><Stat label="Total users" value={c.totalUsers} detail={`${c.activeUsers} active`}/><Stat label="Posts" value={c.totalPosts}/><Stat label="Comments" value={c.totalComments}/><Stat label="Reels" value={c.totalReels}/><Stat label="Suspended" value={c.suspendedUsers}/><Stat label="Deactivated" value={c.deactivatedUsers}/><Stat label="Reports" value={c.totalReports}/><Stat label="User reports" value={c.userReports}/></div><div className="grid gap-4 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between"><h3 className="font-semibold">Recent users</h3><Link href="/admin/users" className="text-sm text-slate-500">View all</Link></div><div className="mt-3 divide-y">{(data.recentUsers||[]).map((u:any)=><div key={u.id} className="flex items-center gap-3 py-3"><div className="h-10 w-10 overflow-hidden rounded-full bg-slate-200">{u.profilePictureUrl&&<img src={u.profilePictureUrl} alt="" className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{u.displayName||u.username||'User'}</p><p className="truncate text-xs text-slate-500">{u.email||'No email'}</p></div><span className="rounded-full bg-slate-100 px-2 py-1 text-[11px]">{u.role}</span></div>)}</div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between"><h3 className="font-semibold">Recent post reports</h3><Link href="/admin/reports" className="text-sm text-slate-500">View all</Link></div><div className="mt-3 divide-y">{(data.recentReports||[]).map((r:any)=><div key={r.id} className="py-3"><div className="flex justify-between gap-3"><p className="text-sm font-medium">{r.reason}</p><span className="text-xs text-slate-400">{new Date(r.createdAt).toLocaleString()}</span></div><p className="mt-1 line-clamp-2 text-xs text-slate-500">{r.post?.content||'Reported post'}</p></div>)}</div></section></div></div>}