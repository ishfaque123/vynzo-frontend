'use client'; import {useEffect,useState} from 'react'; import {deleteAdminPost,fetchAdminPosts} from '@/lib/api/adminApi';
export default function Page(){const[a,setA]=useState<any[]>([]),[q,setQ]=useState(''),[p,setP]=useState(1),[total,setTotal]=useState(0),[confirm,setConfirm]=useState<string|null>(null),[error,setError]=useState('');useEffect(()=>{fetchAdminPosts({page:p,search:q}).then(r=>r.success?(setA(r.data.posts),setTotal(r.data.pagination.total)):setError(r.error?.message||'Unable to load posts.'))},[p,q]);async function remove(){if(!confirm)return;const id=confirm;setConfirm(null);const r=await deleteAdminPost(id);if(r.success){setA(v=>v.filter(x=>x.id!==id));setTotal(v=>Math.max(0,v-1))}else setError(r.error?.message||'Delete failed.')}return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Posts</h2><p className="mt-1 text-sm text-slate-500">Review and moderate published content.</p></div>{error&&<div className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="rounded-2xl border bg-white p-4 shadow-sm"><input value={q} onChange={e=>{setQ(e.target.value);setP(1)}} placeholder="Search post content or author" className="w-full rounded-xl border px-3 py-2 text-sm"/></div><div className="space-y-3">{a.map(x=><article key={x.id} className="rounded-2xl border bg-white p-4 shadow-sm"><div className="flex gap-3"><div className="h-10 w-10 overflow-hidden rounded-full bg-slate-200">{x.user?.profilePictureUrl&&<img src={x.user.profilePictureUrl} alt="" className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><p className="font-semibold">{x.user?.displayName||x.user?.username||'User'}</p><p className="text-xs text-slate-500">@{x.user?.username||'unknown'} · {new Date(x.createdAt).toLocaleString()}</p></div><button onClick={()=>setConfirm(x.id)} className="rounded-lg border border-red-200 px-3 py-1.5 text-xs text-red-700">Delete</button></div><p className="mt-3 whitespace-pre-wrap text-sm text-slate-700">{x.content}</p>{x.imageUrl&&<img src={x.imageUrl} alt="" className="mt-3 max-h-80 rounded-xl object-cover"/>}<p className="mt-3 text-xs text-slate-500">{x._count.likes} reactions · {x._count.comments} comments · {x._count.reports} reports · {x._count.reposts} reposts</p></div></div></article>)}</div><div className="flex justify-between text-sm text-slate-500"><span>{total} posts</span><div className="flex gap-2"><button disabled={p<=1} onClick={()=>setP(x=>x-1)} className="rounded-lg border px-3 py-1.5 disabled:opacity-40">Previous</button><span>Page {p}</span><button disabled={p*20>=total} onClick={()=>setP(x=>x+1)} className="rounded-lg border px-3 py-1.5 disabled:opacity-40">Next</button></div></div>{confirm&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-5 shadow-xl"><h3 className="text-lg font-semibold">Delete post?</h3><p className="mt-2 text-sm text-slate-500">This permanently removes the post and related cascade data.</p><div className="mt-5 flex justify-end gap-2"><button onClick={()=>setConfirm(null)} className="rounded-xl border px-4 py-2 text-sm">Cancel</button><button onClick={remove} className="rounded-xl bg-red-600 px-4 py-2 text-sm font-medium text-white">Delete post</button></div></div></div>}</div>}